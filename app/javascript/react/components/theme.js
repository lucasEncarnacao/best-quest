import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#52BEEB',
    },
    secondary: {
      main: '#F04664',
      contrastText: '#FFFFFF',
    },
  },
});

export default theme;
